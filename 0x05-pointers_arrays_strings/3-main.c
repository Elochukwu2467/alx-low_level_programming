#include "main.h"

/**
 * main - check the code
 *
 * Result: Always 0.
 */

int main(void)
{
	char *str;

	str = "I do not fear computer. I fear the lack of them - Isaac Asimov";
	_puts(str);
	return (0);
}
